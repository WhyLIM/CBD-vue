# PRS 计算路径（gnm_analysis / cluster_matrix）所需运行时依赖。
# 绘图（matplotlib/seaborn）与 GO 富集（goatools）相关 import 已在 Enm.py 中
# 改为懒加载，PRS 路径不会触发，故无需安装。
#
# 注：prody 会自动拉取 numpy、scipy、Biopython。
prody
pandas
networkx
tqdm
